#!/bin/sh
# Used for general, on-startup actions.

# Change background.
feh --bg-scale ~/.dotfiles/background.jpg

# run urxvt daemon
urxvtd&

# emacs setup
export ALTERNATE_EDITOR=emacs EDITOR=emacsclient VISUAL=emacsclient
emacs --daemon&

# Add cabal binaries
export PATH=$PATH:/home/aapollon/.cabal/bin/


